{
  "version": 2,
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}

{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.{js,ts}": {
      "runtime": "nodejs20.x"
    }
  }
}
